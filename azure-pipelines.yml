trigger:
  branches:
    include:
      - Staging
      - master

pool:
  name: Default

variables:
  repoUrl: 'https://github.com/souravb-smu/odoo17.git'
  updateScript: '/home/azureuser/deploy-temp/update_code.sh'
  stagePath: '/home/azureuser/'
  shellpassword: 'Password123@$'

stages:

# =========================
# STAGING STAGE
# =========================
- stage: Staging
  displayName: Deploy to Staging
  condition: eq(variables['Build.SourceBranchName'], 'Staging')

  jobs:
  - deployment: DeployStaging
    environment: Staging
    strategy:
      runOnce:
        deploy:
          steps:

          - task: SSH@0
            displayName: "Sync Code - Staging"
            inputs:
              sshEndpoint: 'ssh-odoo17'
              runOptions: commands
              commands: |
                set -e
                echo "Deploying branch: $(Build.SourceBranchName)"

                cd $(stagePath)
                rm -rf deploy-temp odoo17

                git clone -b Staging $(repoUrl) --quiet
                mv odoo17 deploy-temp

                cd deploy-temp
                rm -f .DS_Store
                rm -rf .github

          - task: SSH@0
            displayName: "Run Update Script - Staging"
            inputs:
              sshEndpoint: 'ssh-odoo17'
              runOptions: commands
              commands: |
                echo "$(shellpassword)" | sudo -S chmod +x $(updateScript)
                echo "$(shellpassword)" | sudo -S $(updateScript)


# =========================
# PRODUCTION STAGE
# =========================
- stage: Production
  displayName: Deploy to Production
  condition: eq(variables['Build.SourceBranchName'], 'master')

  jobs:
  - deployment: DeployProduction
    environment: Production
    strategy:
      runOnce:
        deploy:
          steps:

          - task: SSH@0
            displayName: "Sync Code - Production"
            inputs:
              sshEndpoint: 'ssh-odoo17-production'
              runOptions: commands
              commands: |
                set -e
                echo "Deploying branch: $(Build.SourceBranchName)"

                cd $(stagePath)
                rm -rf deploy-temp odoo17

                git clone -b master $(repoUrl) --quiet
                mv odoo17 deploy-temp

                cd deploy-temp
                rm -f .DS_Store
                rm -rf .github

          - task: SSH@0
            displayName: "Run Update Script - Production"
            inputs:
              sshEndpoint: 'ssh-odoo17-production'
              runOptions: commands
              commands: |
                echo "$(shellpassword)" | sudo -S chmod +x $(updateScript)
                echo "$(shellpassword)" | sudo -S $(updateScript)
